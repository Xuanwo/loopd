syntax = "proto3";
option go_package = "grpc";

service Loopd {
    rpc New (NewRequest) returns (OKResponse);
    rpc Delete (DeleteRequest) returns (OKResponse);
}

message NewRequest {
    int64 id = 1;
}

message DeleteRequest {
    int64 id = 1;
}

message OKResponse {
}
